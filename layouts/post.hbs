{{> header}}

<main class="page-width" role="main">
  <article>
    <header>
      <h1>{{title}}</h1>
    </header>

    {{{contents}}}

    {{#each appended}}
      {{{contents}}}
    {{/each}}

    <footer>
      <time>Posted {{moment date 'Do MMMM YYYY'}}</time>
    </footer>
  </article>
</main>

{{> footer}}
