{{> header}}

<main class="page-width" role="main">
  <header class="header pad-h">
    <a href="/">Alisdair McDiarmid</a> is a senior software engineer based in Toronto, Ontario.
    {{#if header}}<br>{{header}}{{/if}}
  </header>

  <article class="pad-h">
    <header>
      <h1>{{title}}</h1>
    </header>

    {{{contents}}}

    {{#each appends}}
      {{{contents}}}
    {{/each}}
  </article>
</main>

{{> footer}}
